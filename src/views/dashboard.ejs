<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WooCommerce Webhook Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>WooCommerce Orders Dashboard</h1>
    </header>
    <main>
        <section>
            <h2>Recent Orders</h2>
            <table>
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Customer Name</th>
                        <th>Total Amount</th>
                        <th>Status</th>
                        <th>Date</th>
                    </tr>
                </thead>
                <tbody>
                    <% orders.forEach(order => { %>
                        <tr>
                            <td><%= order._id %></td>
                            <td><%= order.customerName %></td>
                            <td><%= order.totalAmount %></td>
                            <td><%= order.status %></td>
                            <td><%= new Date(order.createdAt).toLocaleString() %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        </section>
    </main>
    <footer>
        <p>&copy; <%= new Date().getFullYear() %> WooCommerce Webhook Dashboard</p>
    </footer>
</body>
</html>